<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Zoo Inventory Portal">
    <meta name="author" content="osxi">
    <title>Zoo Inventory Portal</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container" role="main">
      <div class="header">
        <ul class="nav nav-pills small pull-right">
          <li>
            <a href="<%= about_path(@conn, :index) %>">
              About
            </a>
            <a href="<%= animal_path(@conn, :index) %>">
              Animals
            </a>
          </li>
        </ul>

        <a href="<%= page_path(@conn, :index) %>">
          <span class="logo"></span>
        </a>
      </div>
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner %>
    </div>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
